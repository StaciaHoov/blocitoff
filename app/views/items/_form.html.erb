
<%= form_for [user, Item.new] do |f| %>
    <div class='form-group'>
        <%= f.text_area :name, class: 'form-control', placeholder: 'Create New Task' %>
    </div>
    <div class='form-group'>
        <%= f.submit 'Add to list', class: 'btn btn-success' %>
    </div> 
<% end %>
